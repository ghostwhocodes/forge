# Review Only Scripts

The shipped `review-only` workflow currently has no helper scripts.

If future proposal output suggests adding a helper script, place it in this directory and reference it from `workflow.json`.
