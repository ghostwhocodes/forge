Perform an adversarial review of the current implementation state. Compare the request, plan, implementation report, evidence, and quality policy. Find bugs, regressions, unsafe behavior, missing validation, and missing tests. This is a discovery step, not a final acceptance step. Write a structured findings JSON artifact to review_findings and a concise markdown summary to review_summary. Use stable finding ids when the same issue persists across review rounds so later quality-gate rounds can track whether findings are new, resolved, or reopened.
