# QA Gap Guard Samples

These files are sample governance artifacts for the shipped `qa-gap-guard`
template.

They are examples for this repository, not live runtime configuration.

Included samples:

- `qa-governance.json`: example QA governance artifact

For a real repository, prefer a repo-local governance artifact under:

- `.forge/governance/qa-governance.json`
