# Reference Artifacts

This directory contains machine-readable reference material for Forge's current
public-facing shapes.

These files are descriptive reference documents, not necessarily runtime-enforced
contracts.

Current contents:

- `request.schema.json`: reference schema for the normalized request artifact
  shape currently produced by `forge intake`
